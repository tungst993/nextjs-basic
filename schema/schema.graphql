type Mutation {
  createLesson(name: String!, startDate: String!, endDate: String!): lesson!
}

type Query {
  lessons: [lesson!]!
  lesson(id: String!): lesson!
}

type lesson {
  id: ID!
  name: String!
  startDate: String!
  endDate: String!
}
